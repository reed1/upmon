[Unit]
Description=Upmon collector — monitors sites and writes to DB
After=network.target

[Service]
Type=simple
WorkingDirectory={{ app_dir }}/collector-bin
ExecStart={{ app_dir }}/collector-bin/upmon-collector
Environment=RUST_LOG=info
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
