[Unit]
Description=Upmon backend — HTTP API
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/app/upmon/backend-bin
ExecStart=%h/app/upmon/backend-bin/upmon-backend
Environment=RUST_LOG=info
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
