- name: Deploy upmon-agent to target servers
  hosts: localhost
  gather_facts: no

  tasks:
    - name: Run agent deploy script
      ansible.builtin.command:
        cmd: python3 {{ repo_root }}/ansible/playbooks/05_agent/deploy.py
      tags: [push-agent]
